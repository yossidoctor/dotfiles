# Fixture skill

Test fixture for `warn-existing-occurrence.sh`; the token and heading names below are what `cases-warn-existing-occurrence.txt` probes.

## Scope

- **Alpha rule.** Uses `alpha_token` once here.
- **Beta rule.** Names `alpha_token` a second time.
