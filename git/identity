[user]
	email = yossidoctor98@gmail.com

# Scoped to ~/dotfiles via the includeIf in gitconfig (the only ~/dotfiles gitdir).
# git/hooks/pre-commit runs the three commit gates — see README § Commit gate.
[core]
	hooksPath = ~/dotfiles/git/hooks
